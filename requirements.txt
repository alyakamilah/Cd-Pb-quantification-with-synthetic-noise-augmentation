# Python dependencies for CEJA paper code package
# Tested with Python 3.9-3.11
# Install via: pip install -r requirements.txt

# Core scientific computing
numpy>=1.24
pandas>=2.0
scipy>=1.11

# Machine learning
scikit-learn>=1.3
xgboost>=2.0

# I/O
openpyxl>=3.1            # Reading .xlsx files

# Visualization
matplotlib>=3.7

# --- Added for major-revision analyses ---
shap>=0.44            # SHAP feature importance (feature_importance.py); optional —
                      # the script auto-skips SHAP if this is not installed.
